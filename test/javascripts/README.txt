You can find the JavaScript tests at /test/dummy. They are there so that
they can use rails asset pipeline to get jquery and underscore from the
jquery-rails and underscore-rails gems.

To run the JavaScript tests, from root directory:

cd test/dummy
rake jasmine
